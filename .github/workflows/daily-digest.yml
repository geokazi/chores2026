name: Daily Digest Fallback
on:
  schedule:
    - cron: '0 17 * * *'  # Daily 5pm UTC = 9am PST (10am PDT in summer)
  workflow_dispatch:       # Manual trigger for testing

jobs:
  trigger-digest:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger daily digest endpoint
        run: |
          response=$(curl -s -w "\n%{http_code}" \
            "${{ secrets.APP_BASE_URL }}/api/cron/daily-digest?secret=${{ secrets.CRON_SECRET }}")

          http_code=$(echo "$response" | tail -1)
          body=$(echo "$response" | head -n -1)

          echo "Status: $http_code"
          echo "Response: $body"

          if [ "$http_code" -ne 200 ]; then
            echo "::error::Daily digest trigger failed with status $http_code"
            exit 1
          fi
